export class AppUserAuth {

    userName: string;
    bearerToken: string;
    isAuthenticated: boolean;
    canAccessSend: boolean;
    canAccessReceive: boolean;
    canAddUser: boolean;
    canUpdateUser: boolean;
    canAccessAdmin: boolean;
    canAccessReport: boolean;
}

